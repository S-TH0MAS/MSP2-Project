# Copier ce fichier en .env à la racine du projet :
#   cp .env.example .env
#
# Le fichier .env est local à chaque machine et ne doit pas être commité.

# Pseudo GitHub — scripts/lockowners/check-locks.js (npm run lockowners)
# En CI, fourni automatiquement par GitHub Actions.
GITHUB_ACTOR=
